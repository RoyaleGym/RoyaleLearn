{
 "format": 2,
 "why": "S4 (c5a745e) against S3 (3ff1f22), plain arm on RustEngine (past the opening lockout), side B run WITHOUT royaleimitate anywhere (the shared venv has none, no --imitate) and with --forbid-module royaleimitate, so PYTHONPROFILEIMPORTTIME showed neither run child importing it. c5a745e deletes royalelearn/imitation, royalelearn/artifacts.py and the two TRANSITIONAL built-ins (extensions.py _BUILTINS becomes {}), removes the artifact-digest and fit-field-reference subcommands, and makes config._retire drop a null 'imitation' key; none of that runs on a plain config. So everything is identical except the commit: config bytes and hashes, run_id and every identity field, the engine binary, weights, moments, state digests, every metric key and value, the alarm table, its parameters and firings, the checkpoint and other_modules. Measured 2026-09-25 03:49-03:50 with RoyaleGym e09b1bd pinned, RoyaleSim 5ec2b6e, royalesim.pyd e4364b01f3e8.",
 "may_differ": {
  "commit": {
   "why": "side A is 3ff1f22 (S3 with both review rounds), side B is c5a745e (S4); pinned so the file holds only this pair of commits",
   "values": [
    "3ff1f2232d0e9979ba7e1346884407469154baf5",
    "c5a745e8f92b9e215b14bf4ec9febb137a67ba3f"
   ]
  }
 },
 "require": {
  "columns": {
   "commit": "the comparison is between two commits"
  }
 },
 "note": "Nothing else may differ, and the file allows nothing else. Run side B with --forbid-module royaleimitate. The tool's rust_rules probe child (RoyaleGym only, no RoyaleLearn tree) runs without the import profile by design, so the forbid covers the fresh and the resume child, not the probe. Re-measured 2026-09-25 04:04-04:06 with the entry_points column (ab5/base2, RoyaleSim b77440b; every column but environment equals the 03:49-03:50 results): both sides declare no royale* entry point. That the shared venv has no royaleimitate is the sitting's IL control (refused at load_config without --imitate), not a column of this file: a venv-wide install would show on both sides alike."
}
